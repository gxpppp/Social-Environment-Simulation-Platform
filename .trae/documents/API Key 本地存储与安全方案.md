## 方案概述
将 API Key 存储在浏览器本地，不发送到服务端，前端直接调用 Silicon Flow API 验证模型。

## 实施步骤

### 1. 后端修改
- 移除验证接口的 API Key 参数
- 验证接口仅作为代理或完全移除

### 2. 前端加密存储
- 创建加密服务，使用 AES 加密存储 API Key
- 实现 localStorage 加密存取

### 3. 前端直接调用 API
- 修改验证逻辑，前端直接使用本地 API Key 调用 Silicon Flow API
- 添加 CORS 代理或直接使用前端调用

### 4. 用户提示
- 在 API Key 输入框添加提示，说明本地存储
- 添加安全说明

### 5. 清除逻辑
- 监听 storage 事件处理跨标签页同步
- 处理浏览器数据清除

请确认这个方案后，我将开始实施具体的代码修改。